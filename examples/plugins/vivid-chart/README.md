# Vivid chart

The statistics action is a bounded one-shot Python command. The pane writes a
PNG to private temporary storage and invokes the exact `VVMUX_VIVI_BIN` argv
provided by vvmux. Vivid credentials remain in inherited environment and media
bytes remain off the PTY.
