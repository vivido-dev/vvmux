# TypeScript agent utility

The manifest executes the checked-in dependency-free JavaScript build with an
argv array and no shell. The TypeScript source documents the typed authoring
surface. This is trusted native code: its empty broker permission list is not an
OS sandbox.
