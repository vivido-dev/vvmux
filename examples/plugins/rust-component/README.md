# Rust Component hello

This reference uses only the public WIT-backed Rust SDK. Install the Component
target, run `./build.sh`, then link this directory with `vvmux plugin link . --yes`.
The resulting guest has no ambient environment, network, subprocess, or host
filesystem authority.
