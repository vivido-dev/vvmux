# Python dashboard

This is trusted native user code. It exposes a schema-validated one-shot action
and a real PTY pane. Link the directory, inspect the trust/permission preview,
then open `dev.vivido.reference.dashboard/dashboard`.
